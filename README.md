# assistant

Patch — the Milo platform assistant service. The A2A-native runtime for the
AI Agent Framework: it composes per-project knowledge and tools from Service
Catalog agent registrations and serves them to the cloud portal, the `patch`
CLI, and external agents.

Service code, CLI, and e2e environment land via the stacked PRs that follow
this initial scaffold.
