node_modules/
dist/
*.log
.env
.env.local

# Go build artifacts: `go build ./cmd/patch` / `./cmd/assistant` drop these
# binaries in the repo root (cwd). The e2e harness builds into e2e/out/bin.
/patch
/assistant

