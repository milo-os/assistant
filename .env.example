TASK_X_REMOTE_TASKFILES=1

# Local assistant dev (go run ./cmd/assistant): capability endpoints are
# reached over loopback, which the SSRF guard blocks by default.
CAPABILITY_ALLOW_PRIVATE_NETWORKS=true
