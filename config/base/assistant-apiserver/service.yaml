apiVersion: v1
kind: Service
metadata:
  name: assistant-apiserver
  namespace: patch-playground
spec:
  type: ClusterIP
  ports:
    - name: https
      port: 443
      targetPort: 8443
      protocol: TCP
  selector:
    app: assistant-apiserver
